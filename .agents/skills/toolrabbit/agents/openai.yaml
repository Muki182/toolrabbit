interface:
  display_name: "Toolrabbit"
  short_description: "Repo-specific patterns and workflows for toolrabbit"
  default_prompt: "Use the toolrabbit repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true